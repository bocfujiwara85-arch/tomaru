# ── 業務ドキュメント（GitHubに公開しない）──
*.md
翻訳アーカイブ/
翻訳/

# ── 秘密鍵・認証情報（絶対に公開しない）──
ga4-key.json
*-key.json
.env

# ── 内部資料（サイトとして公開しない）──
# 撮影指示書。リポジトリに入れると本番URLで誰でも閲覧できてしまうため除外（2026-07-28）
撮影チェックリスト_5月6月.html

# ── ツール設定（事業情報が混入するため公開しない）──
# 許可リストに過去コマンドが残り、ADR価格帯・内部文書名が読める状態だった（2026-07-28）
.claude/

# ── OS・エディタが自動生成するファイル ──
.DS_Store
Thumbs.db
.vscode/